import React from 'react'
import Model from './Model'

const AddAndUpdateContect = ({isOpen,onClose}) => {
  return (
    <div>
     <Model isOpen={isOpen} onClose={onClose}>
Rathod
    </Model>
    </div>
  )
}

export default AddAndUpdateContect
